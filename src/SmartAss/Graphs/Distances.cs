namespace SmartAss.Graphs;

public sealed class Distances<TNode>
{

    public Distance Distance(TNode from, TNode to)
    {
        return default;
    }

    //private readonly struct Link<TNode> : IEquatable<TNode>
    //{
    //    public readonly TNode From;
    //    public readonly TNode To;

    //    public bool Equals(TNode other) => From.Equals(other.F)
    //}
}
